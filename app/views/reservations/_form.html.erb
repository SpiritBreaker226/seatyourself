<h4>Create A Reservation</h4>

<%= form_for @reservation do |f| %>
<div class="field">
	<%= f.label :time %>
	<%= f.datetime_select :time, prompt: { year: 'Year', month: 'Month', day: 'Day', hour: 'Hour', minute: false} %>
</div>
<%= f.submit %>
<%end%>